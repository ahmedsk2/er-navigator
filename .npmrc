# Lifecycle scripts from dependencies never run on install. The build runs on the
# same Docker daemon as clinical databases; a postinstall script is the one place
# a compromised package could reach it.
ignore-scripts=true
engine-strict=true
