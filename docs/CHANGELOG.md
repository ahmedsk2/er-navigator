# Changelog

One line per finished slice, newest first. The gate reports reference this file.

## Phase 13

- 2026-09-12 [ERN-P13.60] Close Phase 13: the case sheet, redesigned by patient flow. Ahmed asked for five things after the Phase 12 gate: merge "Nursing handover done" into "Left ED"; make the journey times required by outcome and show them together in one block; reorder the sheet by how a patient actually moves through the department, with the times near the top; hide what a case does not need; disclose the rest progressively. One question went back to him and he answered it the same day: at "Open case" only the MRN, the registration time and one delay reason are required, exactly as before. This is the one phase in which the locked plan's "do not redesign the workflow" rule is overridden, by the owner, and only for the shape of the sheet: no taxonomy name is renamed, added or removed, the permission matrix is what it was, and there is no migration. **Delivered:** the spec `docs/specs/phase13-case-sheet.md` [P13.1] and the build [P13.10 to 32]. `src/lib/domain/journey.ts` is the one table of the twelve steps in flow order and of what each outcome owes, and `CaseJourney` is the one block that renders it: "Patient journey", second on the page and on the new-case form alike, in place of the optional "Add journey times" toggle, the "Admission times" section and the transfer chain inside "Referral out". A filled step collapses to one line with an Edit control, the next empty step is the highlighted one and carries the Now button, a step the outcome cannot have is not drawn, a required empty one is tagged "needed to resolve", and a hidden step that holds a time is still listed under "Also recorded", because a hide must never drop a value. "Mark resolved" reads the same table the server refuses on, so the button says what is missing and stays dead until nothing is, and a transfer now owes its receiving facility as well as its tracking number. "Nursing handover done" is gone from the inputs, the workbook, the per-case timeline and the warning chain; `Case.handoverAt` stays as a nullable column that is no longer written, so the cases that already carry one keep it, and the QCH "to ward" column and the Adaa admission band read the departure instead. The five write-up answers sit behind "More to record", and the shift is a chip row in the identity block. **Reviewed:** four lenses over the whole diff with three refuters per finding, 14 raised and 5 confirmed plus one cheap print note, every one fixed with a test that failed first [P13.40 to 45], recorded in [P13.46], with the gate captures re-taken in [P13.47]. **The chain the phase left green:** 1,126 unit and database tests in 66 files, 237 Playwright tests passing at 390 x 844 and 1280 x 800 with 63 project skips, 15 in `tests/instance`, 7 in `tests/demo`, the privilege guard, the build and no schema drift. Deployed by pushing this close commit to `main` as `6a9175c`, fingerprint `38be3187a0f74e5e`, verified on `/api/health` with `/api/ready` returning `ready` and `verify-live` green. The live build then runs one docs commit ahead of these numbers, because recording them is itself a commit ([ERN-P13.61]). The demo at `https://demo-nav.towardpcc.com` runs the same build, fingerprint `7cc481ef29583ab9`, redeployed by hand because it does not follow a push, and its data was reset by a scoped delete and reseeded so that every resolved case satisfies the new rule: ten cases and four accounts, with 25 audit rows of the earlier seed left behind, because `AuditLog` is append-only. One lesson worth keeping: the local chain ran Playwright at five workers and produced load-only timeouts on the heaviest dashboard specs, where CI's two workers are green, so the local chain runs at two now. **Still with Ahmed:** the three questions this phase leaves open, all of them wording or policy rather than code, namely whether the block should be called "Patient journey", whether a death should keep "Disposition decided" optional, and whether "Other" should hide nothing beyond what the stage rules already hide; and everything Phase 12 left with him, none of which has moved: the first admin's password, the repository still being public, the SMTP test send and its `Authentication-Results` check, the staff roster and their accounts in Admin, the hospital authorisation before any real MRN goes off-site, whether the ward Wi-Fi reaches the demo subdomain and behind how many addresses, the first sign-in on the demo, and the eight questions the Phase 12 spec deliberately leaves open.
- 2026-09-12 [ERN-P13.46] The Phase 13 review round: four lenses (correctness, UX, docs, tests) over the whole diff, three refuters per finding, 14 raised and 5 confirmed plus one cheap note, fixed here. [ERN-P13.40] The shift was stamped silently: moved behind the closed "More to record" and left out of the count that opens it, while `blankDraft` fills it from the navigator's last shift and every save re-stamps `lastShift`, so a wrong shift perpetuated itself unseen while the dashboard's `byShift` split and the export column read it; it is back in the identity block after "ED area" as a Morning / Evening / Night chip row, and the section keeps four answers and its promise that nothing recorded is behind a tap. [ERN-P13.41] The reopen trap: decision C stands, so a case resolved before the rule reopens into a state it cannot leave, and "Reopen case" now arms the same two-tap guard as Void over a sentence naming the times that outcome will owe. [ERN-P13.42] The demo seed wrote four times and stopped, so its ADMITTED and TRANSFERRED cases were rows the app itself would refuse to resolve; every outcome's chain is backdated in flow order now, with the receiving facility, and the database test asserts `missingJourneyTimes` is empty and the resolve schema accepts each. [ERN-P13.43] Both guides still described the old sheet and sent a presenter to controls that are not there; rewritten against the running app, with the drift guard in `phase12-spec.test.ts` extended with the Phase 13 sentences so the next drift is caught. [ERN-P13.44] The last fixed 800 ms sleep in the demo kit, the race [ERN-P13.24] fixed in the helper, replaced by the same expect-based wait. And the note, [ERN-P13.45]: the closed "More to record" toggle reveals nothing recorded, so it is `.no-print` until it is opened. The nine refuted findings are not carried: each was answered by an existing test or by the code, and a finding that survives no refuter is not a finding.
- 2026-09-12 [ERN-P13.30] Close Phase 13: the case sheet redesigned by patient flow. Ahmed asked for five things on 12 September (merge "Nursing handover done" into "Left ED"; make the journey times required by outcome and show them together in one block; reorder the sheet by how a patient flows through the ED, with the times near the top; hide what a case does not need; progressive disclosure) and answered the one question the proposal put back to him: at "Open case" nothing new is required, only the MRN, the registration time and one delay reason. The locked plan's "do not redesign" rule is overridden by the owner for this phase, and only for the shape of the sheet: no stage, reason, department, ward, ED area or disposition is renamed, added or removed, and the schema is untouched (`prisma migrate diff --exit-code` reports no drift). `docs/specs/phase13-case-sheet.md` is the contract; `docs/PLAN.md` Section 5 and this log are the record.
- 2026-09-12 [ERN-P13.24] The hands-on demo kit runs end to end again on the redesigned sheet: its patients record their journey in the one block, the shift, the working diagnosis and the payer are entered behind "More to record", and a worked case is reopened step by step before its screenshot. One pre-existing fault of its own was fixed on the way, and it predates this phase: after a first sign-in changes the temporary password, the action deletes every session and issues a fresh cookie, and the kit's fixed 800 ms wait let a `goto` leave with the deleted one, so Omar was bounced to "Your session has ended" before he could open a patient. It waits for the confirmation now. Seven of seven, on the production build over the throwaway demo database.
- 2026-09-12 [ERN-P13.22-23] Two corrections the first full chain run found. The Edit control on a collapsed step used to be drawn only while the sheet was editable, so it vanished for the second a save takes and every row under it moved; it is always drawn now and disabled with the rest. And the new "More to record" test records a working diagnosis rather than a payer: the dashboard suite isolates its own figures with "every payer at once", on the stated grounds that nothing else resolves a case carrying one, and this one moved Adaa KPI 5 from 25 % to 20 %.
- 2026-09-12 [ERN-P13.20-21] The redesigned sheet pinned at both plan viewports and captured. Five tests in `tests/e2e/phase13-case-sheet.spec.ts`: a new case opens on "Patient journey" with the six core steps and nothing else and with "More to record" closed; the admission stage adds its three steps and a referral-number reason adds the transfer's; a step filled with Now collapses to one line, the highlight moves to the next empty one, and Edit puts the box back with its value; "Mark resolved" is dead on a transfer with the whole missing list, then takes the case; a patient who left without being seen is asked for the departure alone, with a physician contact recorded earlier still shown under "Also recorded". No sideways scroll at 390 px with both chains on the page, and the jump strip's chips still land where they did. Four gate captures at both viewports in `design/screens/phase13-*.png`. Three corrections to the spec against the code as built: the shift does not count towards opening "More to record" (every case carries one from `blankDraft`), the resolve schema's non-nullable `departedAt` is the server floor under the departure, and a collapsed step keeps its day.
- 2026-09-12 [ERN-P13.13] The case sheet rebuilt around one "Patient journey" block (`src/components/cases/CaseJourney.tsx`, id `case-times`). The times used to be in four places: an optional "Add journey times" toggle near the foot of the page, an "Admission times" section, the transfer chain inside "Referral out", and "Left ED at" inside Resolve. They are one block now, in the order a patient moves through the ED, second on the page and on the new-case form alike, with every label the taxonomy string it already was. Progressive disclosure: a step with a time is one line, "Triage · 12/09 10:42", with an Edit control that reopens it; the first empty step is the highlighted one; a step the outcome cannot have is not drawn, and a required one carries a "needed to resolve" tag; and a hidden step that holds a time is listed under "Also recorded", because a hide must never drop a value. "Mark resolved" says what is missing before it is pressed. Resolve shows "Left ED" read-only with a Now affordance. The order of the sheet is identity, journey, delay, teams, tests, referral out, "More to record", updates, timeline, resolve, check these times, save; "More to record" holds the shift, the working diagnosis, the payer, pain management and case management, closed on a new case and open on one that already carries any of them. The jump strip keeps its Phase 11 chips and order.
- 2026-09-12 [ERN-P13.12] Required by outcome, enforced server-side. The resolve schema reads the table in `journey.ts` and refuses a resolve that is missing one of the times its outcome needs, on the field's own path: an admission asks for the order and the bed, a transfer for the request and the acceptance, a patient who left without being seen for nothing but the departure, a death for no disposition decision, and OTHER for the triage alone. The receiving facility joins the tracking number on a transfer. The draft schema is untouched, so "Open case" is what it always was. Twenty-three new assertions cover every disposition in both directions; the database suites grew a `worked()` and an `admitted()` that put the required times in order between the registration and the departure, carrying only what an outcome needs, because a bed request and a transfer request are documented actions in the weekly deck.
- 2026-09-12 [ERN-P13.11] `src/lib/domain/journey.ts`, pure and composed from the taxonomy pairs: the twelve steps in flow order, the split into core, admission and transfer, what the block shows before and after a disposition is chosen, what each outcome cannot be resolved without, and which recorded steps a hide would have swallowed. One table, read by the server's refusal and by the editor's mirror, so the two cannot drift.
- 2026-09-12 [ERN-P13.10] "Nursing handover done" merged into "Left ED" (Ahmed's decision A). The step leaves the sheet, the zod input schema, the workbook's Cases sheet, the per-case timeline and the warning chain; `Case.handoverAt` stays in the database, nullable, and no save or resolve writes it again, so the cases that already carry a handover keep it on the record and in their audit snapshots. Its three consumers are repointed at the departure: `isOutOfOrder` and `timeWarnings` check the admission chain as order, requested, assigned; the Adaa admission-to-unit band is the admission order to the leaving time with no fallback, so a resolved case with no departure is in no band rather than banded on a time the app no longer records; and the QCH column "Time of Disposition TO WARD" is the departure for an admitted patient, with the Read me sentence rewritten to say so.
- 2026-09-12 [ERN-P13.1] `docs/specs/phase13-case-sheet.md`: the design as contracts (the exact labels, ids and field names, the required-by-outcome table, the four disclosure rules, the KPI and export consequences), the failing assertion each item starts from, the labels that change with the tests updated for them, and a list of the contracts that stay.

## Phase 12

- 2026-09-11 [ERN-P12.40–48] A review of the merged Phase 12 tree (four lenses — security, correctness, UX, tests — with three refuters per finding) raised thirteen findings and confirmed seven; each is fixed here with a test that failed first, and the six refuted are not carried. **Security:** the demo seed's production refusal could never fire, because `DATABASE_URL`'s host is `db` inside the compose project on production and on the demo alike, and `INSTANCE_LABEL` arrives on the `docker exec` line and says whatever was typed — the only live barriers were the whole-table counts of non-demo cases, and production's is zero. The seed now reads the container's own `APP_URL` through the same guard and refuses a production host whatever else it is handed (missing is refused too), and the headers of `scripts/demo-seed.ts`, `src/lib/demo-guard.ts`, the Dockerfile and the runbook say which refusal is load bearing instead of overclaiming; the runbook says `APP_URL` must **not** be added to the exec line [P12.40]. **Correctness:** the spec still mandated the `x-pathname` header that Slice 12A replaced with `requireUser({ allowMustChange })` — `route-gate.test.ts:164` asserts the header's absence — and now carries the deviation in place beside the specified design, the way the Phase 6 spec records its settings row [P12.41]; the [ERN-P12.30] line below named a download action that has never existed, and the one the route writes, the permission and the audit filter all use is `export.xlsx` [P12.42]. **UX:** the in-flow DEMO banner pushed the sticky desktop rail a banner's height down while it stayed a full viewport tall, so "Account and password" at its foot was clipped at 1280 × 800 with nothing scrolled — the root layout now publishes the banner's height as `--instance-banner` on the same condition that renders it, the banner takes its own height from that variable and the rail subtracts it, and production sets no variable and reads the `0px` fallback, unchanged [P12.43]; the demo script called the case summary's **Copy** button "Copy as text" [P12.44]; and the nurse guide put "+ New case" at the top of the laptop rail, where it sits at the foot, above the signed-in block [P12.45]. **Tests:** the audit-row assertions read the globally newest `report.print` and `export.xlsx` rows, which `instance-banner.spec.ts` and `cases.spec.ts` write as the same supervisor, so the suite was flaky (reproduced on the first attempt) — they are now scoped to the actor and the day through the filters the audit view already has, and matched on their own range and payer rather than `.first()`; the "Failed ×2" cell gets the phone that spec item 6 asked for and the desktop-only acknowledge journey could not give it [P12.46]. The separate intermittent two refuters saw — `[mobile] auth.spec.ts:216` landing on `/login?expired=1` instead of `/account` — did not reproduce: five runs of `auth`, `export`, `cases`, `admin` and `instance-banner` together on both projects, eight executions of that test, all green, so nothing is changed for it and no [P12.47] exists.
- 2026-09-11 [ERN-P12.31] Merge Slice 12C (Opus), the documents: every statement the readiness audit caught having gone stale between Phases 7 and 11 is corrected in place and dated, with what it used to say kept beside it — the runbook said the repository is private and it never was; the backup's third copy does not exist, because the laptop `OracleBackupSync` mirror has never run (P15); Dependabot does not keep the `pnpm.overrides` current (A1); `REPORT_HEADER` is an environment variable, not an editable settings row (P14); the sending mailbox is on the secondary domain, not `navigator@towardpcc.com` (X2); and "still with Ahmed" is down to what is genuinely still with him, four of its six items having been verified done live. New: `docs/guide/nurse-quick-guide.md`, the whole app on one page for the navigator desk, MRN-only first; `docs/guide/demo-script.md`, fifteen minutes in the order of `tests/demo/demo.spec.ts`; the runbook's "Demo instance" section, written as the specification 12B is to build against (its own Coolify project, its own database and volume, new random secrets copied only by name, `INSTANCE_LABEL=DEMO`, `LOGIN_RATE_LIMIT_PER_MINUTE=60`, SMTP left empty on purpose); and the plan's Phase 12 sections, including the three decisions the phase deliberately leaves open — VIEWER's `export.xlsx`, the Dockerfile's node major, and retention [P12.20–24, numbered in a parallel worktree].
- 2026-09-11 [ERN-P12.30] Merge Slice 12A (Opus), the code the readiness audit asked for: `INSTANCE_LABEL` names the copy you are looking at, on the sign-in panel and above every page (`src/lib/instance.ts`, `InstanceBanner`), and production says nothing; the demo seed and `scripts/demo-reset.sh` refuse anything that is not a demo instance (five refusals, an invented `999999…` MRN prefix, `DEV_DB_PORT` read from the environment so the production port is refused); a first password must be changed before anything else — `User.mustChangePassword` (migration `20260911120000_must_change_password`), set on every account an admin creates, enforced at one choke point in `requireUser` with `/account` as the only exemption and an API caller refused 401 before the exemption is read; one MRN-only hint under all five free-text boxes; the login limit is `LOGIN_RATE_LIMIT_PER_MINUTE`, five by default; `export.xlsx` and `report.print` are audited; an alert e-mail that fails carries its attempt count and its last error (migration `20260911120500_alert_email_attempts`, "Failed ×2" in the admin panel) instead of disappearing; patient workbooks are gitignored; three Dependabot bumps taken and the fourth refused, with the reason beside the Dockerfile line it would have changed. The x-pathname redirect the spec specified loops for ever in a server action's own render and was replaced by an explicit `allowMustChange` option, kept to two callers by a test [P12.10–20].
- 2026-09-11 [ERN-P12.60] Close Phase 12: what stands between the app and a staff demo, and between the app and real patients. Ahmed asked the question at the Phase 11 gate and authorised the hosted demo on its own subdomain; a twelve-agent read-only readiness audit answered the first half in 50 items, and `docs/specs/phase12-go-live-readiness.md` [P12.4], reviewed by four lenses with three refuters per finding (21 raised, 9 confirmed, fixed in [P12.4a–f]), was the half Claude owned. **Delivered:** the mark on the printed report masthead and the handover sheet [P12.1]; the playbook rewritten for how Phases 10 and 11 actually ran, Opus does the work and Fable leads [P12.2]; **12A, the code** [P12.10–20] merged as [P12.30]: the `INSTANCE_LABEL` banner on the sign-in panel and above every page, the must-change-password gate at one choke point in `requireUser`, one MRN-only hint under all five free-text boxes, `export.xlsx` and `report.print` audited, the alert e-mail retry with its attempt count and last error, the demo seed and `demo-reset.sh` with their refusals and the invented `999999…` MRN prefix, the workbook gitignore, the configurable login limit and three Dependabot bumps with the fourth refused in writing; **12C, the documents** [P12.20–24, numbered in a parallel worktree] merged as [P12.31]: every statement the audit caught having gone stale between Phases 7 and 11 corrected in place and dated, the one-page nurse quick guide, the fifteen-minute demo script and the runbook's “Demo instance” section; and **12B, the demo instance** [P12.50], live at `https://demo-nav.towardpcc.com` on the same build as production, with production shown untouched. Two fixes at merge time: [P12.32], the demo-seed database test replayed the privileges migration into its own schema and handed the app role UPDATE and DELETE on `AuditLog` and `CaseUpdate` back on `public`, and now re-narrows them; and [P12.33], the five gate captures re-taken from the merged chain. Then a four-lens review of the merged diff with three refuters per finding, 13 raised and 7 confirmed, every one fixed with a test that failed first [P12.40–46] and recorded in [P12.48]. **The chain the phase left green:** 1,063 unit and database tests in 65 files, 221 Playwright tests passing at 390 × 844 and 1280 × 800 with 63 project skips, 15 in `tests/instance`, the privilege guard, the build and no schema drift. Deployed by pushing this close commit to `main` as `258eaed`, fingerprint `89532d505d3eb9b6`, verified on `/api/health` with `/api/ready` returning `ready` and `verify-live` green. The live build then runs one docs commit ahead of these numbers, because recording them is itself a commit ([ERN-P12.62]). Migrations `20260911120000_must_change_password` and `20260911120500_alert_email_attempts` are applied on production, the production login page carries no banner, and `noindex` stays unconditional in the root layout. **Still with Ahmed:** change the first admin's password; make the repository private; the SMTP test send and its `Authentication-Results` check; the staff roster and their accounts in Admin → Users; the hospital authorisation before any real MRN goes off-site; whether the ward Wi-Fi reaches the demo subdomain and behind how many addresses; the first sign-in on the demo, reading its admin password and `DEMO_USER_PASSWORD` off the demo application's own Coolify page; and the eight questions the spec deliberately leaves open.
- 2026-09-11 [ERN-P12.50] **12B, the demo instance, is live at `https://demo-nav.towardpcc.com`.** Corrected in place: this line used to say the demo was not provisioned and that nothing had been created, which was true of the first attempt and is not true now. A Cloudflare A record (`541ec8b3797e4dc1fac94dc6873614d3`, proxied), a Coolify project `demo` (`qs1gv7av4arpnftiayhmnrvh`) and an application `er-navigator-demo` (`iks3t780ppakzvl1nbnvzvfe`) with its own database, its own volume and twenty environment keys: every secret a fresh `openssl rand -hex 24` on the host, no production value reused and each difference checked by `sha256` rather than by printing one, and the five keys the runbook records as dead (`AUTH_SECRET`, `AUTH_TRUST_HOST`, `APP_TIMEZONE`, `LOG_LEVEL`, `ALERT_EMAIL_MAP`) deliberately not copied. It runs the same build as production, fingerprint `03e3b57d858972d2`. `INSTANCE_LABEL=DEMO` puts the undismissable banner on every page and on paper, `LOGIN_RATE_LIMIT_PER_MINUTE=60` lets fifteen people behind one hospital address sign in, `REPORT_HEADER` begins “DEMO.”, and SMTP and `ALERT_PUSH_URL` are blank on purpose, so the worker is log-only and nothing pages anyone for a demo that is switched off. Seeded through `docker exec … node demo-seed.js`: four demo accounts and ten invented patients, MRNs `99999901` to `99999910`, six open and four resolved; a second run in the same minute added nothing. Production was untouched and shown so, the same fingerprint and the same sha256 of its sorted environment key list before and after. The demo does not auto-deploy, because the repository's one GitHub webhook is signed with production's secret and Coolify skips an application whose own secret does not match, so it is deployed by hand; it has no Uptime Kuma monitor and no backup, both deliberate. `docs/RUNBOOK.md` “Demo instance” carries the uuids, the seed command and the reset and delete procedures. Left to Ahmed: the first sign-in, which is a server action and not curl-able.
- 2026-09-11 [ERN-P12.4a–f] A review of the Phase 12 spec (four lenses, three refuters per finding) put up twenty findings and confirmed nine; the eleven refuted include the demo-reset guard's placement, the audit payload's free text and the instance config's missing globalSetup. Every confirmation was against the spec rather than shipped code, so `tests/unit/phase12-spec.test.ts` is the fail-first proof: it reads the document and checks the arithmetic against `app/globals.css`, `docker-compose.production.yml` and `worker/alerts.ts`. Fixed: the DEMO banner takes `bg-band-h4-ink`, since white on `band-h4` is 3.63:1 and `bands.test.ts` has asserted that it fails AA since Phase 9, and it carries `[print-color-adjust:exact]`, without which the printed demo handover sheet is white text on white paper; a caller who must change their password is refused 401 on the API instead of exempted, which had left the whole MRN workbook one bookmarked `/api/export.xlsx` away; item 5 names the two suites that mint an account through Admin → Users and break on the redirect (`tests/e2e/admin.spec.ts`, a CI file, and the Phase 11 demo kit); the demo seed's database test gets a `demo_seed_test` schema of its own, because its whole-table refusals — which must not be scoped away — can never pass on the shared database; and the alert retry pass sends once per cycle within a 60 s budget, since replaying a backlog through the 30 s in-pass sleep would have stalled a cycle past the worker's 900 s heartbeat and paged for a locked mailbox. No code is implemented yet: Slice 12A builds against the corrected spec.
- 2026-09-11 [ERN-P12.1] Put the mark on the printed report masthead and the handover sheet, tone onWhite (a teal tile with a white cross that prints without background colour); the h1 text and every existing selector unchanged; three e2e tests (mark present on both, no horizontal overflow at 390 px) and six gate captures in design/screens/phase12-mark-*.png. Built in a worktree that shares node_modules by junction; note for future worktrees: Turbopack refuses a junctioned node_modules, so either build with --webpack or set turbopack.root to the main checkout.
- 2026-09-11 [ERN-P12.2] Revise the playbook after Phases 10 and 11: Opus does the work, Fable leads (the brief, the gate, tie-breaks); a phase runs in an Opus session; from a Fable session every agent is started with `model: 'opus'`; reviewers at effort `high`. `CLAUDE.md` "Cost discipline" and `docs/PLAN.md` Section 6.

## Phase 11

- 2026-09-11 [ERN-P11.40] Close Phase 11: the plan and this log record the demo, its fixes, the dashboard's new figures and the mark; every gate screenshot is from the final green chain (966 unit and database tests in 58 files; 198 Playwright tests at both viewports, 60 project-specific skips; no schema drift).
- 2026-09-11 [ERN-P11.32–34] A review of the Phase 11 diff (four lenses, three refuters per finding) confirmed seven low-to-medium findings and refuted five; all fixed with tests that failed first: the daily and weekly trends' text equivalent carries each point's median (n<3 included); a tap anywhere in a day's column opens it; the fullest arrival cells print white on teal; the global error page draws the cross; the brand test reads the icon script's code, not its docblock, and a pasted outline fails it; the stay-split test pins each segment. Also: a row updated a moment after the board's clock reads "just now"; the mark grows its bars in the cross's own units so the paths stay open at 28 px; the case strip scrolls sideways on a narrow phone; "Reasons recorded after the decision"; full-page captures start from the top. A tap on a day's bar opens exactly that day again (P11.34: the column-wide tap added in P11.32 read a stale column on a tap with no hover, which the chain's day-drill test caught); a tap elsewhere in the column falls back to it. The hands-on demo is kept as `tests/demo` with `scripts/demo-reset.sh` (runbook, "Hands-on demo").
- 2026-09-11 [ERN-P11.31] Merge Slice 11B (Opus), the dashboard: the Adaa KPIs as bullet charts against their four tiers, to scale; the stay split as 100 % bars overall and per outcome (`phaseSplitByOutcome`); "By day: cases and median stay" on the 7- and 30-day ranges with a 6 h line (`byDay`, drill `day:`); "Arrivals by day and time", weekdays by three-hour blocks (`arrivalGrid`, drill `arrival:`); the bar sections as rows of links with whole labels; seven groups, two columns from `lg` and jump chips on the phone; the Range tile across two columns; stages with no case left out. The report keeps its single column and its order; every new figure takes the filtered cases [P11.20–29].
- 2026-09-11 [ERN-P11.30] Merge Slice 11A (Opus), the fixes a nurse would feel: time rows stack on a phone and show the whole date and time (every one was clipped to "0/2026 10:44 PM"); a sticky "Jump to" strip on the case page (Delay, Teams, Tests, Times, Updates, Resolve); "Open case" pinned at the foot of a new case; every select named by its own label; "Account and password" in the phone menu and on the rail; admin Users as cards on a phone, and no floating New case on admin and account; the sign-in trace below the headline; "Updated just now", and the handover's last update as a clock time [P11.10–19].
- 2026-09-11 [ERN-P11.3] The mark is the logo Ahmed chose from Envato Elements, "Medical Cross Logo — Healthcare & Hospital Health" (3ab2ou): three outlines taken point for point from the template's vector file onto a 48-unit grid (`src/components/brand/mark-paths.json`, read by `Mark` and `scripts/generate-icons.mjs`), recoloured from red to `--color-accent-deep`; the PWA, Apple and favicon icons regenerated; the template's own files kept out of the repository. [P11.2], a heartbeat-and-arrow mark drawn from an Envato AI image, was replaced by it the same morning.
- 2026-09-11 [ERN-P11.1] The hands-on demo and the Phase 11 spec (`docs/specs/phase11-demo-ux-dashboard.md`): on the production build over a fresh local database, a script created four users and took five invented patients through the whole cycle at 390 × 844 and 1280 × 800; its ten findings are the spec.

## Phase 10

- 2026-09-10 [ERN-P10.90] Close Phase 10: the plan, the runbook and this log record the delay fields, the filter, the case summary and "Where the time goes", and every gate screenshot is from the final green chain (920 unit and database tests in 57 files; 174 Playwright tests at both viewports, 60 project-specific skips; no schema drift). The session finished on Opus 5 after Fable's monthly allowance ran out during the review.
- 2026-09-10 [ERN-P10.89] A regression review of the two fix rounds (four lenses over the fix diff, three refuters per finding; it needed three attempts, one stopped by the spend limit and one by agents stalling at maximum effort) confirmed three low findings and brought up one older race, all fixed here with tests that failed first: a dictation the nurse stopped herself that then fails to reach the speech service says why again (the gate from P10.81 now holds back only the button's reset, and the line unless a newer session has started); the retry test looks at the row while the second session is listening, and the late-ending tests fire the stopped session's ending themselves instead of racing a one-second timer; on the board, a slower summary answer no longer replaces the panel asked for after it or reopens one already closed (the host shows only the latest request, and a close cancels what is still on its way).
- 2026-09-10 [ERN-P10.88] When a board summary's row has gone by the time it is closed, the keyboard goes to the page title (`PageHeader` gained an optional `headingId`, focusable from a script and never a Tab stop), not to the search box, which raised the phone's keyboard over the list. Two comments now say the summary is as of the last render: every case action revalidates the page.
- 2026-09-10 [ERN-P10.85–87] Second review round (Opus, three worktrees, each fix proven by a test that failed first). The board: a handover sheet printed from a filtered or searched board says how under its stamp line ("Filtered: …", "MRN search: …"), so a partial list never reaches the next shift unmarked; one summary host in `Board` draws the panel outside the list, so the 30 s poll dropping a row no longer closes a summary mid-read; an unreadable summary's message sits on its own line under the row; the filtered-poll test now checks the poll's answer, and the summary tests read the visible rows rather than the hidden copy text [P10.70–74]. The filter: a reason every stage carries ("Other") is offered once, under "Any stage", instead of ten chips that lit together; on the phone the panel's dim closes from its own tap, which can no longer land on the Filter button, a chip's × or "Clear filter"; the export count and the workbook share one filter adapter (`toFilterableCase`), tested for every dimension; the browser suite now takes the live filtered count through its route, clicks a filtered dashboard's drill links, and presses Exclude and the lone finding [P10.75–79]. The editor: one clock helper (`caseClockOf`) for the summary and the header, so a resolved case whose "Left ED at" was cleared stops at its resolution everywhere; a dictation session that ends late no longer stops the one tapped on after it; the unreachable-speech-service line is reworded; the update-count check was added before the not-quoted check (the finding that the summary was stale after an update was wrong: every case action revalidates the page) [P10.80–84].
- 2026-09-10 [ERN-P10.65–67] First review round (Opus, three worktrees). A seven-lens adversarial review of the merged tree (Fable ran six lenses before its allowance ran out; Opus re-ran the seventh and the refuters) found seventeen defects and test gaps and one non-finding (`parseCaseFilter` carrying unknown names, which the spec asks for, like `parseDrill`). Fixed, each with a test that failed first: the printed report's drill links keep its filter; a filter that matches nothing says so instead of "No cases yet."; the filter is described by dimension ("or", "and" under the lone finding, "Excluding cases with … and …"); a filtered workbook carries a "Case filter" row on its Summary or read-me sheet and a `_filtered` filename [P10.50–54]; on a phone a tap on the case summary's backdrop no longer opens the row or follows "Back" beneath it; a resolved case whose departure time was cleared is summarised as having left at its resolution; the summary route's session and voided-case guards are tested [P10.55–58]; the dictation boxes are labelled by id so the microphone is not part of their name, a blocked, missing or silent microphone says why in one line, dictation stops at each box's cap (now also its `maxLength`), the update box fills its row again, and the Phase 10 sign-ins have their own client addresses on the login limiter [P10.60–63]; the phase drill ids, the board's unfiltered denominator, the reopened case and the transfer category are pinned by tests.
- 2026-09-10 [ERN-P10.35] Merge Slice 10C (Opus): the case summary, the weekly deck's ranked-table row plus its time sequence for one patient, with Copy as text. A quiet "Summary" button in the case page header and a 44 px button on every board row (a sibling of the row's link, never inside it, so the three specs that count `a[data-mrn]` are unchanged) open it from the new `GET /api/cases/[id]/summary`; a bottom sheet on the phone, a centred dialog on a laptop; Escape, an outside tap and Close leave it and focus returns to the button. No update text and no resolution note, in the panel or on the clipboard: `src/lib/cases/summary.ts` is pure and its test asserts the omission over a fixture that deliberately contains a name. The desktop label strip reserves an empty sixth column so the five headings stay over the cells they name (`tests/e2e/surfaces.spec.ts` now measures the alignment directly); the phone row's reason line spans the full card [P10.30–34].
- 2026-09-10 [ERN-P10.25] Merge Slice 10B (Opus): the filter bar on the board, the dashboard and the export page: stage, reason, ED area, department, CTAS, payer and disposition, include or exclude, "among others" or "the lone finding", carried in the address so a filtered view is a link. One predicate (`src/lib/domain/case-filter.ts`) serves all three pages, so the board's rows, the dashboard's population and the export's count and workbook cannot disagree about what a filter means: the board's counters, empty state and handover sheet are over the filtered rows and the bar reads "{shown} of {total} open cases", with the 30 s poll keeping the filter; the dashboard filters before a single number is computed (headline, total and the previous period over one population, a footnote saying which); the export's live count and workbook are narrowed by the same predicate over the same window and `/report` prints the filter in its masthead. An empty filter emits no query keys, so every link the app produced before this phase is unchanged. `CaseForStats` gained `reasonNames` and `areaCode`; the export's own `reasons` select gained the stage `code` it was missing, without which the workbook's phase split had no stage rows [P10.20–24].
- 2026-09-10 [ERN-P10.42] "By payer" and the phase drill-downs proven in the browser once the seed records a payer. [P10.41] The phase-split tests tightened where two independent verifiers showed a mutant would survive (per-phase median guard, the tie charged once to the earlier phase, longestN summing to completeN, a voided case that carries times, stages and a payer proven absent from every row); the spec's names aligned with the module. [P10.40] "Where the time goes" in the KPI module (`phaseSplit`: front end = door to physician, decision = physician to disposition decision, after the decision = decision to leaving for resolved cases; per-phase medians, shares over the cases with all three measured, the longest phase per case, the stage rows under each phase) and `byPayer`; drill sections `phase` (grid key `phase|median`, `phase|longest`, `phase|<stage>`) and `payer`; the two dashboard sections, on the screen after Stay bands and after By ED area, on the report after Working targets. Hand-computed tests on the fixture, two independent recomputations agreeing on every figure.
- 2026-09-10 [ERN-P10.16] Merge Slice 10A (Opus): the working diagnosis (one line, 80 characters, the identifier warning) and the payer (Government · Insured · Self-pay) through the draft, the rules, the audit snapshot, the editor (after CTAS and after the ED area), the board row (a payer chip and a diagnosis line), the handover sheet (under the MRN) and the workbook's Cases sheet (two columns after ED area) [P10.10–13]; the in-app microphone (`DictationButton`, Web Speech API where the browser offers it, on the Other boxes, the update box, the diagnosis and the resolution note; `Permissions-Policy` now `microphone=(self)`) [P10.11]; the browser proof and the `phase10-fields` captures [P10.14–15]. The QCH sheet has no column for either and is unchanged.
- 2026-09-10 [ERN-P10.1] Phase 10 spec (`docs/specs/phase10-delays.md`, from a four-reader code map) and the data kit: the `Payer` enum, `Case.diagnosis` and `Case.payer` (migration `20260910140000_diagnosis_payer`, nullable, no grant), stage codes and the payer on `CaseForStats` and `KpiCase` with every fixture updated, `PAYER_LABELS`.

## Phase 9

- 2026-09-10 [ERN-P9.44] Phase 9 closed: the plan's Section 0, 1 and 5 and this changelog record the refresh; the full chain on the merged tree is green (809 unit and database tests, 129 Playwright at both viewports with 45 viewport skips, privilege guard, build) and every gate screenshot is from that run. Deployed the same day.
- 2026-09-10 [ERN-P9.43] Merge Slice 9C (Opus): the board's rows are cards and the 6 px threshold stripe becomes the elapsed clock itself, filled with the band's colour behind white text (`BAND_PILL`) — one `<a>`, two templates: three stacked lines beside the pill on the phone, and MRN · Registered · Waiting on · Last update · Elapsed under an `aria-hidden` label strip on a laptop (140 px first column so an MRN and two identity chips share a line) [P9.31]; every dashboard section is a card with an `aria-hidden` icon in its heading, the `<h3>`-first and table-sibling structure the tests read untouched, and the headline tiles gain the tinted 28 px square [P9.32]; the case editor's sections become the same card with their own icons, its chips take the soft tint (a case with nine reasons chosen was nine teal rectangles) and the two button fills are exported [P9.33, P9.35]; the export page drops its hand copies of the input and button classes for the primitives and becomes a card at a reading width [P9.34]; on paper the cards go back to strips [P9.36]; every gate screenshot re-taken and four `phase9-surfaces-*` captures added, with `scripts/screenshots.mjs` waiting for a visible h1 [P9.37, P9.39]; Lighthouse mobile signed in: board 97/100, `/cases/new` 97/100, an open case 95/100, CLS 0, in `design/screens/phase9-lighthouse.json` [P9.38]. Tests first in `tests/e2e/surfaces.spec.ts`, asserting geometry and computed colour rather than class names [P9.30]. Cards sit at the 16 px gutter every other Phase 9 surface uses (the spec said 12).
- 2026-09-10 [ERN-P9.42] The refusal screen's brand is counted by visible heading (the header's h1 is hidden from `lg` since P9.40); every gate screenshot refreshed under the merged shell. [P9.41] The green band bar on the sign-in panel is lightened like the other three; the tokens file records the four illustration tints and the icon decisions. [P9.40] The brand is drawn once per screen (the header's mark and h1 are `lg:hidden`, the rail's wordmark is the h1 there, the bar names the department) and "+ New case" sits in the rail on a laptop instead of floating over the admin tables.
- 2026-09-10 [ERN-P9.14] Merge Slice 9A (Opus): sign-in is direction A — a 300 px teal hero with the wordmark as the page's one h1, the headline, the hospital line and the ECG trace, under a white sheet at `--radius-sheet`; from `lg` a 46/54 split with the four band bars as the illustration and the form in a card. Fields, ids, names, errors and the session-ended notice unchanged; a Show/Hide password toggle added [P9.11]. The PWA icons are rasterised from the mark with `sharp` (devDependency pinned to the lockfile's 0.35.4); the icon is the solid mark because the outline blobs below 64 px, and the Apple icon is painted flat for iOS's own mask [P9.12]. A `forbidden()` thrown below a layout renders inside it, so the in-shell refusal has its own boundary without the wordmark band, sharing the copy with the root one through `src/components/holding.tsx` [P9.13]. Tests first: the hero at both viewports, the card's geometry on desktop, the toggle, and `tests/unit/colour-literals.test.ts` keeping the four illustration tints the only colour literals in the app [P9.10]. The account page takes both slices' changes at the merge: the PageHeader and two cards at a reading width.
- 2026-09-10 [ERN-P9.26] Merge Slice 9B (Opus): one navigation, two shapes — the bottom tab bar gained icons and grew to 64 px on the phone, and from `lg` the same `nav[aria-label="Sections"]` is a 232 px navy rail with the wordmark above and the signed-in user below [P9.21]; the signed-in shell is a two-column grid on a laptop, so every page uses the width instead of a 448 px column; the header is the teal gradient with the mark, and the overflow trigger shows the user's initials (same "Menu" name, same three items, now with icons); every page title goes through one `PageHeader` and the admin chips gained icons [P9.22]; the printed handover sheet is the width of the paper again after the desktop grid had squeezed it, held by a Playwright test [P9.23]; every gate screenshot re-taken under the new chrome plus four `phase9-shell-*` captures [P9.24]; the drill-down's back button lines up with the rows [P9.25]. Tests first for the rail's geometry, the initials trigger and the initials rule [P9.20].
- 2026-09-10 [ERN-P9.1] Phase 9 spec (`docs/specs/phase9-visual-refresh.md`, from a five-reader code map of every selector the suite pins) and the identity kit: the heart-and-trace mark and wordmark (`src/components/brand/Mark.tsx`), twenty-four Lucide 1.43.0 line icons copied in under the ISC licence (`src/components/icons.tsx`, no dependency), the navy rail and hero tokens with `.bg-hero` and `.bg-header`, and one Band → class map (`src/components/bands.ts`, with `BAND_PILL` for the elapsed-time pill, white on `band-h4-ink` never on `band-h4`) replacing three drifting copies, with a contrast test.

## Phase 8

- 2026-09-10 [ERN-P8.74] Runbook: the two Uptime Kuma monitors exist (`ER Navigator — ready`, keyword on `/api/ready`; `ER Navigator — worker`, push every 900 s), `ALERT_PUSH_URL` is set in both Coolify copies, and the Kuma service's compose gained an unauthenticated Traefik router for `/api/push/` because the UI's basic auth answered 401 to the worker's pushes.
- 2026-09-10 [ERN-P8.73] A screen that stayed open across a deploy reloads itself: the root layout stamps `<html data-build>` with the build fingerprint, and the error boundary compares it with `/api/health` and reloads when they differ, with an "ER Navigator was updated" notice; a Reload button joins Try again and Board for every other error. Found when the login page from the previous build answered "Something went wrong" after the morning deploy (app log: `Failed to find Server Action`; no audit row). Unit test on the decision, smoke test that the page stamp equals the probe header.
- 2026-09-10 [ERN-P8.72] Adversarial check of P8.70 (one verifier, six angles, the change confirmed): a missing hour is now a blank cell rather than an empty text cell, which Excel charts plot as zero and COUNTA counts; a round-trip test streams a workbook and reads it back to assert the number, its "0.00" format, the blank and the "n<3" text, in both the tabular and the free sheet. [P8.71] Runbook: the owner-role re-hash for an ADMIN_PASSWORD changed after the first seed (done on production 10 September 03:54 UTC).
- 2026-09-10 [ERN-P8.70] The navigator workbook's hour columns (total ED hours, hours waiting so far, order to bed, consult to seen and to reply, order to result, and the Summary medians) are numeric cells with the "0.00" format rather than the text "8.00", so Excel can sum, average and chart them (Ahmed's decision of 10 September); "n<3" stays text. The Adaa and QCH sheets are unchanged: their times are the forms' own hh:mm text.
- 2026-09-09 [ERN-P8.69] Refreshed screenshots from the final Phase 8b chain and the plan's Section 0 wording for the review. [P8.68] The admin area-reorder test creates its own neighbour to swap with: the suites share one database and run in parallel, and a move "up" from the end of the area list once swapped with the cases suite's temporary area and hit P2025 when that suite deleted it between the read and the update.
- 2026-09-09 [ERN-P8.67] Phase 8b review fixes (four adversarial lenses over the finished code: 2 of 14 findings confirmed, both fixed). C1: the Adaa workbook leaves the painkiller offset and time (columns K and N) blank when the dose fell on the Riyadh day before the registration, as it already does for triage, keeps the Yes/No answers and the dose, and counts such rows in the Read me. C2: `CaseUpdate.system` marks the notes the app appends itself (the resolve, reopen and void notes; the alert threshold notes), migration `20260910130000_system_updates` adds the column and backfills those rows once as the owner, and the stats loader leaves them out of the action kinds and the untagged count — so a resolved case with no navigator note is "No action documented" rather than "Update without an action tag". The Adaa panel's KPI 7 row is labelled "Mortality".
- 2026-09-09 [ERN-P8.66] Merge Slice H (Opus, `docs/specs/phase8b-decisions.md`). [P8.65] Gate screenshots `phase8b-dashboard` and `phase8b-report` at both viewports. [P8.64] Database and browser suites over every Slice H cell and panel, on a fixture case carrying all of the collection decisions at once. [P8.63] The export read selects the update's action tag, so an exported case carries its documented actions. [P8.62] The QCH workbook fills the two discharge answers, the whole case-management block in the sheet's own words, Reviewed By and the MRI image type, and writes Deceased and Referred to UCC verbatim in both decision columns; Case Manager Name stays blank. [P8.61] The Adaa workbook fills columns I–N from the pain block, writes Deceased and Referred to UCC as discharge types, appends KPI 7 (% deceased over every tracked case) and KPI 8 (door to painkiller, total minutes) to the CTAS table, lays the Pain Killer Statistics block out per CTAS with its two denominators, and rewrites the Read me lines that stopped being true. [P8.60] The Adaa panel gains KPI 7 (ungraded) and KPI 8 (graded on the minutes benchmark), the pain bands beside the pethidine doses with their denominators stated, "Actions documented" and "Documentation" render the module's seven and nine rows, a "Discharge communication" section shows both answers as share bars, new drill sections `painkiller` (bands and doses on the pipe) and `communication`; `/report` carries all of it.
- 2026-09-09 [ERN-P8.55] After the Slice G merge: the painkiller and case-management times wired into the two timeline loaders (case page and board), the panel names for KPI 7 and KPI 8, and the test literals widened.
- 2026-09-09 [ERN-P8.54] Merge Slice G (Opus, `docs/specs/phase8b-decisions.md`). [P8.46] `untaggedUpdatesCount` on the loader, the field the KPI contract grew after the slice began. [P8.45] Tests: validation over the dose rules, the two answer vocabularies, the case-management enums, MRI, the two new dispositions and the update category; warnings over the painkiller and case-management order; thirteen database tests over create, save, clear, two resolves, `addCaseUpdate` with and without a category, and `reviewCase` marked, re-marked, refused for a navigator with its audit row, cleared by a save and a resolve, left standing by an update, a reopen and a void; four Playwright tests. [P8.44] The editor records them: an MRI row; an optional "Action taken" chip row above the update box in the deck's six categories, shown as a chip on each tagged update; "Pain management (Adaa KPI 8)" and "Case management" sections; "Instructions given by doctor" and "Family engaged" in the resolve block, whose disposition select carries Deceased and Referred to UCC; a supervisor's "Mark reviewed" with "Reviewed by <name>, <time>" for every role and a "Reviewed" chip on resolved board rows. [P8.43] The stats loader widened to the 8b contract (the updates read loses its `take: 1` so the distinct categories can be counted). [P8.42] Rules and services: the draft carries the twelve new fields, "Not sure" refused on the pain answers and accepted on the discharge ones, pethidine dose 50/100/150 only with a pethidine Yes, painkiller time only with a painkiller Yes; a painkiller before registration and a case-management reply before the call are warnings; the audit snapshot carries every column and the review; `addCaseUpdate` takes an optional category, stored with the row and never changed; `reviewCase` requires `case.review`, is idempotent, audited, neither version-checked nor version-bumping, and is cleared by `saveCase` and `resolveCase` only. [P8.41] The data model: `Disposition` gains Deceased and Referred to UCC, `InvestigationType` gains MRI, five new enums, sixteen nullable `Case` columns, `CaseUpdate.action`, `User.reviewedCases`; migration `20260910120000_collection_decisions` proven on an empty database and on one migrated from the previous chain with a resolved case, an update and a CT row surviving; `case.review` for SUPERVISOR and ADMIN in the matrix with its audit action.
- 2026-09-09 [ERN-P8.53] The 8b verification finished: the documentation list carries the painkiller and pethidine gap rows, the spec contract matches the module, the 60-minute band case sits where the form puts it.
- 2026-09-09 [ERN-P8.52] The 8b verification applied (workflow `ern-kpi-verify-8b`, two verifiers and an adjudicator, 8 raw, 5 confirmed): untagged updates are counted by the loader rather than inferred from distinct kinds; KPI 7 divides deaths by every tracked case in the group, open included, as the form divides by total patients; `painkillerYesN` and `pethidineYesN` state the true denominators of the bands and doses; the painkiller band names state their bounds ("&gt;30 min–1 h").
- 2026-09-09 [ERN-P8.50] The KPI module for the collection decisions (`docs/specs/phase8b-decisions.md`): KPI 8 door-to-painkiller (only when one was prescribed) with the definitions sheet's benchmark (1 h / 3 h / 5 h), the form's painkiller bands and pethidine doses, KPI 7 mortality as Deceased among resolved cases, UCC referrals and the sickle-cell count on the Adaa summary row; "actions documented" reworked into the weekly deck's six categories with the recorded escalation, fax and transfer timestamps folded in and an "update without an action tag" row; discharge communication shares; a "resolved, not yet reviewed" completeness row; MRI in the investigation order, the timeline and the turnaround bands; the two new dispositions labelled; the case-management call order and the painkiller time in the out-of-order check. 39 module tests; the dashboard, drill and e2e expectations that named the old action rows updated.
- 2026-09-09 [ERN-P8.40] `docs/specs/phase8b-decisions.md`: Ahmed's answers to the brief's eight questions (A no; B, C, D, G, H yes; E Deceased and Referred to UCC, no LAMA; F yes) as a data model, rules, editor, KPI contract and export spec, in three slices.
- 2026-09-09 [ERN-P8.38] Phase 8 adversarial review applied (workflow `ern-phase8-review`, five lenses, one skeptic per finding, 26 agents; 21 findings examined, 4 confirmed, all fixed): the Adaa "Admission Type" and the QCH ward-time cells follow the outcome, so a ward left on a patient who was then discharged is no longer pasted into the national form as an admission; a triage on the Riyadh day before the registration (which the form cannot represent) keeps its cells blank and is counted on the Read me for hand entry; the investigation-order warning compares each recorded step with the last recorded one, so a report typed before the scan is still flagged when the new preliminary field is empty; the "no computable stay" row and footnote name the cause that actually reaches the database (a leaving time before registration). Seventeen findings refuted on the evidence (payload growth, the login in the QCH sheet, the mean-based colouring on the summary sheet, the empty-range panels) are left as they are.
- 2026-09-09 [ERN-P8.36] Merge Slice E (Opus). [P8.23] The dashboard fixture gains a CTAS, an ED area, the journey milestones, an imaging preliminary read, updates, an escalation, a transfer request and one MRN with two visits; every new section, footnote, chart and drill-down is asserted on named MRNs; the timeline is checked on the case page and on the print sheet; gate screenshots `phase8-dashboard`, `phase8-timeline`, `phase8-report`, `phase8-handover` at both viewports. Found on the way: "Within" and "n" collided at 390 px (now one column) and Recharts 3 orders a stack's legend by series registration (now a server-rendered legend in band order). [P8.22] The weekly deck's per-case slide, generated: a read-only "Timeline" section after the updates on the case page (every recorded instant with its Riyadh clock time and the gap from the step before) and the same sequence in one compact line under each case on the handover print sheet; the timeline rides on `BoardRow` so the sheet matches the rows the board shows. [P8.21] The dashboard and the printed report get the deck: seven headline tiles with their delta against the previous period (the longest stay linked to its case), the stay-band histogram, the stage bars relabelled "Pathways", the Adaa KPI panel with treated-within bands and the quoted footnote, the working targets with a share bar and a drill-down to the misses, admission to unit, the turnaround stack, exam to consult, the ten longest stays, actions documented, outcomes replacing the disposition bars, by CTAS, by ED area, repeat visits and the documentation checks; a section with nothing to show renders nothing; `src/lib/dashboard/panels.ts` holds all the formatting; benchmark colours are the ok / accent / 4 h / 6 h tokens and every cell names its band in words; `/report` opens with the four sections a reader of the deck looks for first. [P8.20] `dashboard()` gains `kpi`: every figure a `kpi.ts` call (the only arithmetic added is the "10 h or more" and "12 h or more" shares, over `measured` and MIN_N-guarded), the previous period measured to `previousRange().asOf`, twelve new drill sections with the two grid sections carrying both coordinates in one pipe-joined name.
- 2026-09-09 [ERN-P8.34] Merge Slice F (Opus). [P8.33] Tests for the two new workbooks: the Adaa row asserted as a literal twenty cells for a case recorded across Riyadh midnight and for an open case with only a registration; the QCH row as a literal seventy cells; the summary sheet's CTAS rows, "n<3" shares and benchmark fills; every statement each Read me makes; `tests/db/export.test.ts` drives the real handler for all three formats and `export.spec.ts` downloads each in the browser and opens it with exceljs. [P8.32] The QCH navigator sheet workbook (`format=qch`): the August log's seventy columns in its order and spelling, two header rows derived from one column list, no patient name column; imaging rows fill the two blocks in CT, ultrasound, X-ray order, consults oldest first; every delay-reason column is the case's taxonomy reasons for that stage; durations `h:mm`; comments are the updates as `HH:mm text`; the ward carries "/ ISOLATION"; columns the app does not record are present and blank, listed in the Read me. [P8.31] The Adaa ED KPIs workbook (`format=adaa`): sheet `ED KPIs manual` with the official form's columns A–T, headers and vocabulary (Riyadh `DD-MMM-YYYY` and `hh:mm`, calendar-day offsets blank at zero, admission type from `unitTypeOf` with PICU and NICU kept, discharge type Home / DAMA / Another Health Facility, the pain block blank), one row per case; `KPI summary` laying `adaaSummary()` out as the form's ED Statistics block with benchmark colours and a key; a `Read me` with the population, the rows without a CTAS, the door definition, KPI 6 as a DAMA lower bound, the 36-second admission-band difference and the paste range. Nothing recomputed outside `kpi.ts`. [P8.30] The export format: `ExportFormat` on `ExportRange`, `?format=` on both route handlers, a Format select with one line of help each on `/export`, per-format filenames, the refusal audit row carrying the format; `workbook.ts` generalised to free-form and tabular parts with group headers and per-cell fills; four cell formatters in `format.ts`.
- 2026-09-09 [ERN-P8.14] Merge Slice D (Opus, `docs/specs/phase8-reports.md`). [P8.6] `roomAt`, `transferAcceptedAt` and `transportArrivedAt` added to the stats loader after `KpiCase` grew them, so the KPI module's input is complete. [P8.5] `CaseForStats` widened to the reports spec's `KpiCase`: the journey milestones, the admission chain, the ward code, CTAS, the ED area and the update counters, all from one query; the Cases sheet gains CTAS and ED area after Shift, the Investigations sheet gains Preliminary report. [P8.4] The case editor records CTAS and the ED area as two single-select chip rows after the shift, and a "Preliminary report" time on each imaging row between "Scan done" and "Reported"; a retired area behaves like a retired reason or ward, and an official report before the preliminary one warns rather than blocks; the board row and the handover sheet show CTAS and the area as small chips after the MRN. [P8.3] Admin → Reference lists gains "ED areas" with add / rename / reorder / deactivate, audited as `list.update` on `EdArea`. [P8.2] Three collection fields the ED reports ask for: `Case.ctas` (1..5, bounded by zod), `Case.areaId` with the new Admin-editable `EdArea` list, and `CaseInvestigation.preliminaryAt`; migration `20260910090000_ctas_area_preliminary`, hand-generated with `prisma migrate diff` and proven twice on an empty database; `ED_AREAS` seeded insert-if-missing and explicitly not Appendix A; `EdArea` joins the PHI guard's reference models.
- 2026-09-09 [ERN-P8.13] Duplicated contract line dropped from the reports spec.
- 2026-09-09 [ERN-P8.12] The KPI contract in the reports spec brought in line with the verified module (required transfer and room fields, `previousRange` returning `asOf`, six completeness rows, the official report for the imaging target, band labels as coded).
- 2026-09-09 [ERN-P8.11] The KPI module after its three-verifier check (workflow `ern-kpi-verify`, 26 raw findings, 20 adjudicated, all applied): one door (earlier of registration and triage) and one leaving time (`endAt`) everywhere, so KPI 1, KPI 5, the admission bands and the timeline agree and a reopened case is open in every figure; the imaging working target measures the official report; the previous period is measured to its own end (`asOf`), never to today; completeness gains "open 24 h with no disposition decided" and "registration in the future"; timeline keys are unique and ties break by a fixed pathway order rather than load order; CTAS rows 1..5 always present; min and max are facts, only medians and means are guarded; boundary, edge and voided-only tests added (34 tests).
- 2026-09-09 [ERN-P8.10] `src/lib/domain/kpi.ts`: the numbers the weekly delayed-tickets deck, the monthly ER Journey deck and the Adaa form compute, over tracked cases — stay bands, headline, longest stays, actions documented, outcomes, completeness, repeat visits, the per-case timeline, Adaa KPI 1–6 with the definitions sheet's benchmarks, treated-within and admission-to-unit bands, the August sheet's five working targets, exam-to-consult, turnaround bands, by CTAS and by area. Pure functions over `KpiCase` with a hand-computed fixture.
- 2026-09-09 [ERN-P8.1] `docs/specs/phase8-brief.md` (what the four ED files measure, what the app already holds, the dashboard ideas taken in, the two export designs, the collection-field gaps and the eight decisions for Ahmed) and `docs/specs/phase8-reports.md` (the KPI contract and Slices D, E, F).

## Phase 7

- 2026-09-09 [ERN-P7.33] Merge Slice A of the final review (Opus, `docs/specs/phase7-review-fixes.md`). [P7.17] The RSC-bypass e2e assertion matches how Next 16.3.4 actually refuses a flight request: the response is 200 with a `NEXT_HTTP_ERROR_FALLBACK;403` row and none of the page's data, while a document request gets the real 403 asserted on `page.goto`. [P7.16] The board's 30 s poll no longer treats a 401 as a dropped packet: it clears the interval and navigates to `/login?expired=1`, and a muted "Updated HH:MM" / "Not updating since HH:MM. Check the connection." line under the counts strip says how old the rows are (C6, C19). [P7.15] Every `/admin` page and the dashboard check the caller themselves, because Next skips ancestor layouts on an RSC request and the shared admin layout was the only gate on four of six admin screens; a signed-in NAVIGATOR was shown to receive the staff directory and the audit table that way (C1, high). [P7.14] `requireRole` alongside `requireAction`, used by the admin layout and the two admin screens with no action in the locked matrix, so they refuse with a real 403 and an `auth.forbidden` audit row. New `tests/unit/page-guards.test.ts`.
- 2026-09-09 [ERN-P7.32] Merge Slice B of the final review (Opus). [P7.24] Stale-version 409 tests for `resolveCase` and `voidCase`, so every version-guarded mutation has one. [P7.23] A team's consult times survive a department chip deselect and re-select within the session, as in the prototype (C18). [P7.22] A thrown server action in the case editor and the admin panels now shows a persistent `role="alert"` "Could not reach the server. Nothing was saved. Check the connection and try again." instead of a button that silently re-enables (C11). [P7.21] `loadReferenceForCase`: a case stays editable after Admin deactivates a reason, department or ward it carries; the retired row renders as a greyed deselect-only "(retired)" chip and the rules accept it only on the case that already has it, `createCase` stays strict (C4, C10). [P7.20] The resolve schema is derived from the refined case rules, because "Mark resolved" bypassed the Other-text, requiresDepartment, duplicate-row, unknown-reason and future-registration checks that "Save changes" enforces (C5).
- 2026-09-09 [ERN-P7.31] Restore-into-production drilled on the live database (planned outage 13:58 to 14:03 UTC, 5 min 10 s): fresh dump, Coolify Stop, `pg_restore --clean --if-exists --single-transaction` from a throwaway `postgres:16-alpine` on the volume, Coolify Start. Learned and written down: Stop removes the built db image and Start is a full deployment; the restore re-grants the app role DELETE on `AuditLog` until the deployment's `sync-app-role` runs, so the privilege check after Start is mandatory.
- 2026-09-09 [ERN-P7.30] Final review, lead slice (findings C2/C3, C7, C9/C13, C12, C14, C15/C16, C17 and the refuted-but-true items; `docs/specs/phase7-review-findings.md`): the weekly chart applies the n<3 rule (`src/lib/dashboard/weeks.ts`: a week with fewer than three cases plots a gap and tooltips "n<3", footnote says so); the post-login `next` path is resolved against a placeholder origin instead of pattern-matched, so `/<TAB>//evil.example` no longer redirects off-site (`src/lib/auth/next-path.ts`, 20 unit cases); the login username is lower-cased like Admin's and the seed's; `fmtHours`/`spokenHours` carry 60 minutes into the hour ("6h 60m" is gone); the worker touches the heartbeat only after a cycle that completed and GETs an Uptime Kuma push URL (`ALERT_PUSH_URL`) after every successful one; the SMTP transport gets 10/10/20 s connection/greeting/socket timeouts; the server-action guard no longer counts the module that defines `assertCan` as a guarded service and refuses arrow-function actions; CI asserts all eight revoked privileges of the app role; the PHI guard covers every model except `User` (reference lists may have a `name`); dead configuration removed (`AUTH_SECRET`, `AUTH_TRUST_HOST`, `APP_TIMEZONE`, `LOG_LEVEL`); runbook gains a restore-into-production procedure, drilled on production.
- 2026-09-09 [ERN-P7.12] The live security audit's remaining code items: session and remember cookies renamed `__Host-ern_session` and `__Host-ern_remember` with `Secure` unconditional (the prefix makes the browser refuse the cookie unless it is Secure, `Path=/` and Domain-less, so nothing else under `*.towardpcc.com` can plant or shadow a session); `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Resource-Policy: same-origin` added and Permissions-Policy widened (display-capture, serial, bluetooth, hid, midi, xr-spatial-tracking, motion sensors, browsing-topics, interest-cohort all denied); the SMTP transport insists on STARTTLS on 587 (`requireTLS`) and never negotiates below TLS 1.2, with a unit test on the transport options. `headers.spec.ts` asserts the new headers; the route-gate test pins the new names.
- 2026-09-09 [ERN-P7.8] `pnpm audit` cleared: 2 high and 2 moderate advisories, all transitive and none reachable from app code — `mysql2` (auth-plugin downgrade to `mysql_clear_password`, and an unbounded zlib inflate) and `deepmerge-ts` (stack exhaustion) through the `prisma` CLI, `uuid` (missing buffer bounds check) through exceljs. `pnpm update` could fix none of them (prisma 7.10.0 is the newest 7.x and pins `mysql2` at exactly 3.15.3; prisma 8 is a release candidate), so they are pinned with `pnpm.overrides`: `deepmerge-ts ^8.0.0`, `mysql2 ^3.23.1`, `uuid ^11.1.1`. Because `deepmerge-ts` crosses a major inside the tool that runs migrations, the whole production path was re-proven on an empty database afterwards (generate, `migrate deploy`, `sync-app-role`, seed, `migrate diff`, and `pnpm install --frozen-lockfile`). `pnpm audit --audit-level=moderate`: no known vulnerabilities.
- 2026-09-09 [ERN-P7.7] Gate screenshots refreshed at both plan viewports: the Phase 3 and Phase 4 captures predated the tab bar's role check and still showed an Export tab to a navigator. New `design/screens/phase7-install-*` (the install banner in both shapes) and `phase7-lighthouse.json`. The board's own captures dismiss the banner first, because the phone project's user agent is Safari's and it would otherwise appear in all four.
- 2026-09-09 [ERN-P7.6] `tests/unit/server-actions.test.ts`: every function exported from an `app/**/actions.ts` file must reach `requireAction`, or reach `requireUser`/`getSession` and hand the actor to a service that calls `assertCan`. Signing in, signing out and changing your own password are exempt by name, with a reason, and the count of exemptions is asserted. Mutation-checked: removing the `requireUser()` from the admin actions' helper fails the test by name.
- 2026-09-09 [ERN-P7.5] Lighthouse mobile, signed in, on the production build. Case editor (`/cases/new` and an open case): performance 96 → 96, accessibility 98 → 100, best practices 96 → 100. Board: performance 94 → 98, accessibility 100 → 100, best practices 100 → 100. Fixed: the missing `<main>` on the case editor (it sits outside the `(app)` shell); a CSP `eval` violation from zod 4's JIT probe (`z.config({ jitless: true })`); 0.121 of cumulative layout shift on the board, all of it the install banner appearing above the rows after hydration (moved below the list, where it displaces nothing); and an accessible name that did not contain its button's visible text. Plus, from the plan's accessibility list: every elapsed clock now carries the duration in words for a screen reader (`spokenHours`, "6 hours 5 minutes" where the tabular text reads "6h 05m"), and one visible focus ring for every link, button and chip is stated once in `app/globals.css`. Method, numbers and the audits deliberately not fixed (unused JavaScript from the React/Next runtime, Next's own legacy polyfills, the render-blocking stylesheet, `bf-cache` — every page is `no-store` on purpose — and SEO, which is `noindex` on purpose) are in `design/screens/phase7-lighthouse.json`.
- 2026-09-09 [ERN-P7.4] PWA: `app/manifest.ts` serves ER Navigator / "ER Nav" at `/manifest.webmanifest` (standalone, start url `/`, theme `#1f7a8c`, background `#f5f7f6`, 192 and 512 icons offered as both `any` and `maskable`); the icons are drawn in code by `scripts/generate-icons.mjs` — a white ring and two hands on a rounded square in the accent colour, supersampled and encoded with nothing but `node:zlib`, no image library and no template artwork; the root layout adds the manifest, icon, apple-touch-icon and iOS standalone metadata, and `proxy.ts` keeps all of those paths public because a phone fetches them before anyone signs in. The board gains a dismissible install banner: an Install button wired to the `beforeinstallprompt` event on Chrome and Edge, one line about Share → "Add to Home Screen" on iOS Safari, dismissal remembered in `localStorage` (a UI preference, the one thing the locked plan allows there, and never case data).
- 2026-09-09 [ERN-P7.3] CSP without `'unsafe-inline'` for scripts: the whole policy moves out of `next.config.ts` into `proxy.ts`, the only place that can mint a per-request nonce. Sixteen random bytes go onto the request as `x-nonce` and as `content-security-policy` (which is where Next reads the nonce for its own bootstrap scripts) and onto the response as the served policy; `script-src` is now `'self' 'nonce-…' 'strict-dynamic'` with `object-src 'none'` added. Verified: every `<script>` Next emits carries the nonce and the nonce differs per request (`tests/e2e/headers.spec.ts`). `style-src` keeps `'unsafe-inline'`, and only Recharts is why — measured by serving `style-src 'self'` and walking the app: the board, the case editor and the admin screens reported no violation at all, while `/dashboard` reported twenty inline-style refusals and rendered no chart, because Recharts sets `style="…"` attributes and a style attribute cannot carry a nonce.
- 2026-09-09 [ERN-P7.2] A refused page is a real HTTP 403: `experimental.authInterrupts` is on, `app/forbidden.tsx` is the body, and `requireAction()` calls Next's `forbidden()` after writing the `auth.forbidden` audit row. A VIEWER on `/cases/new` and a NAVIGATOR on `/export` or `/report` now get a status a proxy, a log and a monitor can see instead of a 200 whose body says no. Route handlers and server actions are unchanged.
- 2026-09-09 [ERN-P7.1] The alert directory moves onto the user record: `User.email` (nullable, unique, migration `20260909150000_user_email`), set and cleared from Admin → Users on create and per row, validated with zod (trimmed, lower-cased, max 254) and audited as `user.update` with the address in before and after. The alerts worker's recipients become the active SUPERVISOR and ADMIN rows that have one, read from the database every cycle, so a change on Admin → Users takes effect within an interval with no redeploy. `ALERT_EMAIL_MAP` and `parseRecipientMap()` are deleted outright — code, `.env.example`, compose passthrough, entrypoint allowlist and runbook. The PHI guard still reads only the `Case` model and now says why: `User.email` is contact data for a member of staff who signs in, never a patient's.

## Phase 6

- 2026-09-09 [ERN-P6.7] Admin and the threshold-alerts worker: `worker/alerts.ts` runs as its own container (same runner image, `node worker.js`, esbuild bundle at `dist/worker.js`, `internal` network only, heartbeat healthcheck, `depends_on migrate`), scanning the OPEN cases every `ALERT_INTERVAL_MINUTES` and, the first time one passes 4, 6, 12 or 24 hours, writing the `Alert`, the seeded `system` user's "Reached {t}h threshold" update and the `alert.fire` audit row in one transaction — the unique index on `(caseId, thresholdHours)` makes a restart or a second worker a no-op — then emailing the active supervisors and admins from 6 h up through nodemailer (`SMTP_*`; empty `SMTP_HOST` logs the message instead, a send failure is retried once after 30 s and never thrown out of a cycle, recipients are looked up every cycle and their addresses come from `ALERT_EMAIL_MAP` because the locked data model has no address column), touching `/tmp/heartbeat` at the end of every cycle and offering `--test <address>` for the DKIM/DMARC check. `/admin` replaces the placeholder with five screens (`/admin/users`, `/admin/lists`, `/admin/other`, `/admin/alerts`, `/admin/audit`), ADMIN-only and desktop-first: users with one-time temporary passwords, role changes, deactivation and password resets that delete sessions, and refusals for self-deactivation, self-demotion and any change to the `system` account; departments, wards and per-stage reasons with add, rename (id kept, so tagged cases stay tagged), deactivate and up/down; the prototype's Other queue with Promote (creates or reuses the reason under its stage, re-tags the case, moves the primary across, bumps `version`) and Dismiss; fired alerts with an Acknowledge control, mirrored in the case editor's header for SUPERVISOR and ADMIN; and the append-only audit log with action/entity/actor/date filters, pagination and a changed-keys diff. Everything audited (`user.create`, `user.update`, `user.password`, `list.update`, `other.promote`, `other.dismiss`, `alert.fire`, `alert.acknowledge`); nothing anywhere is deleted. Also fixed: a cookie whose session had been deleted (an Admin deactivating someone) bounced between the route gate and the page — `requireUser()` now redirects to `/login?expired=1` and the gate clears the cookie there. 487 unit and database tests (413 without a database), 72 Playwright checks, screenshots in `design/screens/phase6-*`.

## Phase 5

- 2026-09-09 [ERN-P5.5] The Excel export and the printed report: `/export` replaces the Phase 3 placeholder with the prototype's `ExportPanel` (From/To over the registration date defaulting to the last seven Riyadh days, an Open/Resolved/All filter, the live "{n} cases in range" from `GET /api/export/count`, Download Excel disabled at zero, Print report opening `/report` in a new tab, and the prototype's footnote); `GET /api/export.xlsx?from&to&status` streams the workbook with exceljs's `WorkbookWriter` — Summary (generated at, range, status filter, tiles, threshold table, by shift, by disposition, medians still obeying n<3), then Cases, Consults, Investigations and Updates with the prototype's columns exactly, `dd/mm HH:mm` in Asia/Riyadh, two-decimal hours, blank for missing, a bold frozen header, widths from the header and a sample, and the filename `ER_Navigator_{from}_to_{to}.xlsx`; `/report?from&to&status` prints the hospital header (`REPORT_HEADER`, defaulted in compose and on the entrypoint allowlist), the range, the Riyadh generated-at, the requesting user and every dashboard section, reusing the extracted `DashboardBody` and the Phase 4 print stylesheet, with a Print button and no shell. Both are SUPERVISOR, ADMIN and VIEWER: a NAVIGATOR gets 403 and an `auth.forbidden` audit row from the routes and the refusal screen from the pages, and the Export tab is hidden for them. `src/lib/export/` (Riyadh calendar-day range and bounds, pure row builders, one `findMany` widening the dashboard's own select, the streaming writer, the two route cores). A successful export is logged at info level with the actor and the range, not audited — the audit table is for changes. 408 unit and database tests (354 without a database), 63 Playwright checks, screenshots in `design/screens/phase5-*`.

## Phase 4

- 2026-09-09 [ERN-P4.5] The dashboard: `/dashboard` is now the prototype's `Dashboard` section for section on one `dashboard()` call — title and "{inRange} of {total} cases", the 7/30/90/all range chips (`?r=`), the three tiles with the n<3 rule, the threshold table and its footnote, the weekly chart, primary reason, stage, departments, the consulted-team, investigation, admission-chain and shift tables with n<3 per row and the prototype's empty-state copy, weekday and disposition bars, and the Other review queue linking to its case; every bar and table row drills down through `?drill=<section>:<name>` resolved against the same aggregates (unknown or out-of-range keys fall back to the dashboard) and the case list is the board's own `BoardRowItem`. `src/lib/cases/stats-mapper.ts` (Prisma → `CaseForStats`, distinct stage names, Other text with its stage), `src/lib/dashboard/{drill,load}.ts` (one `findMany` for every non-voided case), `loadBoardRowsByIds()` on the board loader, Recharts 3 client components with the token colours guarded by a test against `app/globals.css`, and the `@media print` dashboard sheet Phase 5 reuses for `/report`. Only the charts need JavaScript; every bar chart also ships its rows as a hidden link list, so drill-downs work by keyboard and with scripting off. The weekly section is small multiples rather than the prototype's dual axis — one scale per chart, per the spec and the `dataviz` skill. 353 unit and database tests (311 without a database), 57 Playwright checks at both viewports with twelve seeded cases across twenty-nine days, screenshots in `design/screens/phase4-*` including the print sheet. Second Lighthouse mobile run, on `/dashboard`: performance 91, accessibility 100, best practices 96 (no gate until Phase 7).

## Phase 3

- 2026-09-09 [ERN-P3.1] The board and the handover sheet: `/` is now the prototype's board (server-rendered rows sorted by elapsed hours with the threshold band, MRN and registration stamp, primary reason and consulted teams, the "No update for {h}" staleness line amber from two hours, the elapsed clock, the whole row a 44 px link to the case); MRN search stripped to digits and Open/Resolved/All both persisted in the URL (`?f=&q=`); the counts strip over the open cases whatever the filter shows; a 30 s poll through `GET /api/board` (401 without a session, VOIDED never returned) with `now` ticking on the same beat; `src/lib/board/` (pure `rows.ts`, one-query `load.ts`) and the reusable `BoardRowItem`; the app shell gains the bottom tab bar (Board, plus Dashboard, Export and — for ADMIN — Admin placeholders), the floating "+ New case" button (hidden for VIEWER, absent on `/cases/*`) and an overflow menu holding the signed-in name, "Print handover" and Log out; the `@media print` handover sheet ("Qatif Central Hospital, Emergency Department. ER Navigator handover", Asia/Riyadh date-time, the printing nurse, a seven-column hairline table); the route gate now answers a signed-out `/api/*` with 401 instead of redirecting. 297 unit and database tests (259 without a database), 41 Playwright checks with eight seeded cases across every band, screenshots in `design/screens/phase3-*` including the print sheet. First Lighthouse mobile run on the board: performance 95, accessibility 100 (no gate until Phase 7).

## Phase 2

- 2026-09-09 [ERN-P2.1] The case vertical slice: `/cases/new` and `/cases/[id]` with the prototype's editor section for section (identity and registration with the quick chips, stage and reason chips with Other text and the primary selector, departments and consult chains, investigation, admission and transfer chains, journey times, append-only updates, resolve/reopen, the "Check these times" panel); six server actions in `app/cases/actions.ts` over `src/lib/cases/service.ts` (create, save, add update, resolve, reopen, void) with optimistic locking, the 409 "changed by" payload from the newest audit row, the pending `OtherReview` lifecycle and one before/after audit row per mutation; read-only rendering for VIEWER and for voided cases; reference-data loader wired into `buildCaseSchemas`; UI atoms under `src/components/ui/`; 270 unit and database tests (237 without a database), 38 Playwright checks at both viewports, screenshots in `design/screens/phase2-*`. Opening a case takes 8 UI actions including sign-in.

## Phase 1

- 2026-09-09 [ERN-P1.8] Lead review and merge: remembered devices now slide the browser-side cookie lifetime on every request (route gate re-stamps `ern_session` and `ern_remember`), runbook NUL byte removed and the CI NUL guard extended to docs, nested worktrees excluded from lint/tests/tsc. Verified after merge: 236 unit and database tests, 22 Playwright tests, typecheck, lint, build.
- 2026-09-09 [ERN-P1.6] Sign-in, sessions, lockout and the role gate: `Session` model and migration (sha256 of an opaque 32-byte cookie, 12 h sliding expiry, rotated on login and password change, deleted on logout and deactivation); `src/lib/auth/` (password with a dummy-hash timing path, session, lockout at 10 failures for 15 minutes, in-process 5-per-minute IP rate limiter, login and change-password cores); `proxy.ts` route gate on cookie presence only; `/login` with remember-this-device; the authenticated shell at `/` with the band legend and logout; `/account` change-password; `auth.login`, `auth.logout`, `auth.fail`, `auth.locked`, `auth.forbidden` and `user.password` audit rows; 187 unit and database tests (171 without a database), 22 Playwright checks at both viewports, screenshots in `design/screens/phase1-*`.

## Phase 0

- 2026-09-09 [ERN-P0.9] Gate 0 closed: design doc v3 filed and reconciled, repo public for the build, SMTP sender recorded (blocked on towardpcc.com SPF; plan section 9 item 3), Opus as the implementation tier. Phase 0.3 tokens extracted from the Envato items into `design/tokens.md`, `app/globals.css` and `app/layout.tsx` (IBM Plex Sans via next/font, type ramp, radii, shadows); contrast test extended.
- 2026-09-08 [ERN-P0.4] Review fixes: Prisma client cached in production (was one pool per query); seed is insert-if-missing so Admin edits survive deploys; `prisma/sync-app-role.ts` re-applies the app role's password and privileges on every deploy; app entrypoint strips every env var not on its allowlist (Coolify injects all of them); `band(null)` is the neutral `none` band as in the prototype; amber band token `#B8790F` plus a text variant with a contrast unit test; PHI guard regex broadened; `docker-compose.dev.yml` and a CI e2e job with the production Postgres image; Playwright web server; screenshot script refuses blank captures; `scripts/backup.sh`; healthcheck start intervals; runbook rewritten for stop-then-start deploys, failed-migration recovery, secret rotation order, volume guard rails and a PHI scrub procedure; plan Section 1 records the sequencing, session-design, seed-contract, deploy-model and amber decisions and asks two more Gate 0 questions.
- 2026-09-08 [ERN-P0.3] First production deploy on Coolify (commit ac3672f, fingerprint cf6c356bcc69ff4b): db healthy, migrate exited 0 (2 migrations, seed: 10 stages, 48 reasons, 16 departments, 8 wards, 1 admin), app healthy as uid 100 with the ernav_app role; https://nav.towardpcc.com/api/ready returns ready through Cloudflare. GitHub push webhook 676338800 wired for push-to-deploy.
- 2026-09-08 [ERN-P0.1] Scaffold: Next.js 16, TypeScript strict, Tailwind 4 with prototype tokens, Prisma 7 schema (locked plan section 3, relation-complete), Appendix A seed, duration formulas ported from the prototype with unit tests, PHI-guard test, security headers, `/api/health` (build fingerprint) and `/api/ready` (SELECT 1), Dockerfile (deps/build/migrate/runner), production compose (db + migrate + app, isolated Postgres, two DB roles), CI, Dependabot, plan and runbook.
